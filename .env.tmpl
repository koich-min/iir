# Example environment configuration for local or container use.
# Copy this file to .env to activate.
#
# This file contains NON-SECRET configuration values.
# Secret values must be placed in .env.secret.

# --------------------------------------------------------------------
# Django runtime settings
# --------------------------------------------------------------------

# Enable Django debug mode (true / false)
# WARNING: must be false in production
#
# Parsed as:
#   DEBUG = os.getenv("DJANGO_DEBUG", "False").lower() == "true"
#
# DJANGO_DEBUG=false


# Comma-separated list of allowed hostnames (NO spaces)
#
# Parsed as:
#   ALLOWED_HOSTS = os.getenv("ALLOWED_HOSTS", "localhost").split(",")
#
# Examples:
#   ALLOWED_HOSTS=localhost
#   ALLOWED_HOSTS=localhost,127.0.0.1
#   ALLOWED_HOSTS=iir.example.com
#
# ALLOWED_HOSTS=localhost


# Comma-separated list of trusted origins for CSRF (NO spaces)
#
# Parsed as:
#   CSRF_TRUSTED_ORIGINS = os.getenv("CSRF_TRUSTED_ORIGINS", "").split(",")
#
# Required when using HTTPS via Ingress.
#
# CSRF_TRUSTED_ORIGINS=


# --------------------------------------------------------------------
# Database configuration (non-secret)
# --------------------------------------------------------------------

# Database engine: sqlite (default), postgresql, or mysql
#
# DB_ENGINE=sqlite


# Database host (postgresql / mysql only)
#
# DB_HOST=localhost


# Database port (optional)
# Defaults:
# - PostgreSQL: 5432
# - MySQL:      3306
#
# DB_PORT=5432


# Database name (postgresql / mysql only)
#
# DB_NAME=iir


# Optional sqlite database file path (when DB_ENGINE=sqlite)
#
# SQLITE_PATH=/data/db.sqlite3

